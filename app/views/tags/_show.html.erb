<h2>Tags</h2>
<div>
  <% @post.tags.each do |tag| %>
    <span style="display: inline-block; margin-right: 10px; padding: 5px; border: 1px solid black;">
      <%= tag.name %>
    </span>
  <% end %>
</div>

