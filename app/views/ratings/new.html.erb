<!DOCTYPE html>
<html lang="en">
<head>
  <style>
      body {
          background-color: #f0f0f0;
          font-family: Arial, sans-serif;
          display: flex;
          justify-content: center;
          align-items: center;
          height: 100vh;
      }

      .container {
          background-color: #fff;
          border: 1px solid #ccc;
          padding: 20px;
          text-align: center;
      }


      .btn-primary {
          background-color: #007bff;
          color: #fff;
          padding: 10px 20px;
          font-size: 16px;
      }

  </style>
</head>
<body>
<div class="container">
  <%= form_for [@post.topic, @post, Rating.new], html: { class: "form" } do |f| %>
    <h2>Rate this post</h2>
    <div class="form-group rating">
      <% (1..5).each do |value| %>
        <%= f.radio_button :value, value %>
        <%= label_tag "rating_#{value}", "#{value} star" %><br><br>
      <% end %>
    </div>
<br>
    <%= f.submit "Submit Rating", class: "btn btn-primary" %>
  <% end %>
</div>
</body>
</html>
